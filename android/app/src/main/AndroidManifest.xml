<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Release builds need this explicitly: only the debug/profile manifests
         declare it, so a release APK had no network at all — remote images and
         the REST API both failed silently. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Completion proof: a workshop photographs the finished job (and, on a
         part-and-fitting job, the part's own box) before the escrow may be
         released. Only the camera needs declaring — picking an existing photo
         goes through the system photo picker, which hands back a single file
         the user chose and needs no gallery-wide permission. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <!-- Android 13+: needed before the app may show a notification for an inbox
         row pushed over its live connection to the API (/hubs/notifications).
         Requested at sign-in; refusing it only hides the system banner. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- The background push service (lib/core/push/background_push.dart): a
         foreground service that keeps the notification connection open after
         the app is closed. Android 14+ requires a declared type and its
         permission; remoteMessaging has no daily time limit and may start
         after a reboot. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING"/>
    <application
        android:label="AK Cars"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- flutter_background_service declares this service; the app adds the
             foreground service type the permission above names. -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="remoteMessaging"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.DIAL"/>
            <data android:scheme="tel"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
